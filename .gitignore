# Dependencies (local-only; the static site needs no build)
node_modules/

# Wrangler local cache (legacy Cloudflare Worker tooling)
.wrangler/

# cPanel FTP deploy state (created on the server)
.ftp-deploy-sync-state.json
.ftp-deploy-apex-state.json

# Local working source for the sibling sites (each has its own repo)
/aevrium bios project/
/fareban holdings project/
/kura project/
/nevulium project/

# Local design scratch (not part of the deployed site)
design-direction.html
aevrium_biosystems_investor_website_final.html

# OS / editor
.DS_Store
Thumbs.db
*.log

# Local scratch — temp render/build scripts and screenshots
_*.mjs
_*.png
shot-*.png

.dev.vars*
!.dev.vars.example
.env*
!.env.example
