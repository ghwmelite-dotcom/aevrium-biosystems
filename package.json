{
  "name": "aevrium-biosystems",
  "version": "1.0.0",
  "private": true,
  "description": "Aevrium Biosystems — investor website (Cloudflare Worker: static assets + form API).",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "start": "wrangler dev --port 8788",
    "deploy": "wrangler deploy",
    "static": "npx --yes serve public -l 5050"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
